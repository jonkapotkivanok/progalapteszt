const összeadás = (n1, n2) => {
  res = n1 + n2;
  return res;
};

const szorzás = (n1, n2) => {
  res = n1 * n1;
  return res;
};

const hatványozás = (n1, n2) => {
  res = n1 ** n2;
  return res;
};

module.exports = {
  összeadás,
  szorzás,
  hatványozás,
};
